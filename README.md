# Krake Test

A simple playground to test the Krake data harvesting engine with

### Running
To run in development mode
```console
forever -w -c coffee krake_test.coffee
```

### Setting up
```console
git clone git@github.com:KrakeIO/test-server.git
cd test-server
npm install coffee-script -g
npm install forever -g
npm install
```